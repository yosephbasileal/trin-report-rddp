'use strict';

var React = require('react');
var mui = require('material-ui');

// Empty components for placeholding
var Empty = React.createClass({
  render: function() {

    return (
      <div>

      </div>
    );
  }
});


module.exports = Empty;